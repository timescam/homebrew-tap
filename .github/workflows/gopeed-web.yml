name: gopeed-web

on:
  schedule:
    - cron: '0 0 * * *'  # Runs daily at midnight UTC
  workflow_dispatch:      # Allows manual trigger

jobs:
  update-formula:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT }}
          fetch-depth: 0  # Need history to check current version

      - name: Get current version
        id: current
        run: |
          CURRENT_VERSION=$(grep -m 1 'version' Formula/gopeed-web.rb | cut -d '"' -f 2)
          echo "version=$CURRENT_VERSION" >> $GITHUB_OUTPUT

      - name: Fetch latest version
        id: fetch-release
        run: |
          RELEASE_INFO=$(curl -s "https://api.github.com/repos/GopeedLab/gopeed/releases")
          VERSION=$(echo "$RELEASE_INFO" | jq -r .[0].tag_name | sed 's/^v//')
          
          # Check if the version is null or empty
          if [ -z "$VERSION" ] || [ "$VERSION" = "null" ]; then
            echo "Error: Invalid version detected. Aborting workflow." >&2
            echo "version=" >> $GITHUB_OUTPUT
            exit 1
          fi
          
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Download and calculate SHA256
        if: steps.current.outputs.version != steps.fetch-release.outputs.version && steps.fetch-release.outputs.version != '' && steps.fetch-release.outputs.version != 'null'
        id: download
        run: |
          VERSION="${{ steps.fetch-release.outputs.version }}"
          
          # Get download URLs and calculate SHA256
          ARM_URL="https://github.com/GopeedLab/gopeed/releases/download/v${VERSION}/gopeed-web-v${VERSION}-macos-arm64.zip"
          X86_URL="https://github.com/GopeedLab/gopeed/releases/download/v${VERSION}/gopeed-web-v${VERSION}-macos-amd64.zip"
          
          ARM_SHA256=$(curl -sL "$ARM_URL" | sha256sum | cut -d ' ' -f 1)
          X86_SHA256=$(curl -sL "$X86_URL" | sha256sum | cut -d ' ' -f 1)
          
          echo "arm_sha256=$ARM_SHA256" >> $GITHUB_OUTPUT
          echo "x86_sha256=$X86_SHA256" >> $GITHUB_OUTPUT

      - name: Delete old branches
        if: steps.current.outputs.version != steps.fetch-release.outputs.version && steps.fetch-release.outputs.version != '' && steps.fetch-release.outputs.version != 'null'
        run: |
          git branch -r | grep 'origin/gopeed-web-' | while read -r branch; do
            branch=${branch#origin/}
            git push origin --delete "$branch"
          done
        env:
          GH_TOKEN: ${{ secrets.PAT }}

      - name: Update Formula and README
        if: steps.current.outputs.version != steps.fetch-release.outputs.version && steps.fetch-release.outputs.version != '' && steps.fetch-release.outputs.version != 'null'
        run: |
          VERSION="${{ steps.fetch-release.outputs.version }}"
          ARM_SHA256="${{ steps.download.outputs.arm_sha256 }}"
          X86_SHA256="${{ steps.download.outputs.x86_sha256 }}"
          
          # Update version in Formula
          sed -i "s/version \".*\"/version \"${VERSION}\"/" Formula/gopeed-web.rb
          
          # Update ARM SHA256
          sed -i "/on_arm do/,/end/ s/sha256 \".*\"/sha256 \"${ARM_SHA256}\"/" Formula/gopeed-web.rb
          
          # Update x86_64 SHA256
          sed -i "/on_intel do/,/end/ s/sha256 \".*\"/sha256 \"${X86_SHA256}\"/" Formula/gopeed-web.rb
          
          # Update ARM URL
          sed -i "/on_arm do/,/end/ s|url \".*\"|url \"https://github.com/GopeedLab/gopeed/releases/download/v${VERSION}/gopeed-web-v${VERSION}-macos-arm64.zip\"|" Formula/gopeed-web.rb
          
          # Update x86_64 URL
          sed -i "/on_intel do/,/end/ s|url \".*\"|url \"https://github.com/GopeedLab/gopeed/releases/download/v${VERSION}/gopeed-web-v${VERSION}-macos-amd64.zip\"|" Formula/gopeed-web.rb
          
          # Update README version (assuming there's a gopeed-web entry in README)
          sed -i "s/\`gopeed-web\`.*|\s*\`[0-9.]*\`\s*|/\`gopeed-web\` | \`${VERSION}\` |/" README.md

      - name: Create Pull Request
        if: steps.current.outputs.version != steps.fetch-release.outputs.version && steps.fetch-release.outputs.version != '' && steps.fetch-release.outputs.version != 'null'
        id: cpr
        uses: peter-evans/create-pull-request@v5
        with:
          token: ${{ secrets.PAT }}
          commit-message: "chore: update gopeed-web to version ${{ steps.fetch-release.outputs.version }}"
          title: "Update gopeed-web to version ${{ steps.fetch-release.outputs.version }}"
          body: |
            Automated update of gopeed-web to version ${{ steps.fetch-release.outputs.version }}
            
            - Updated version to ${{ steps.fetch-release.outputs.version }}
            - Updated SHA256 checksums for both ARM and x86_64 builds
            - Updated version in README.md
          branch: gopeed-web-${{ steps.fetch-release.outputs.version }}
          delete-branch: true

      - name: Enable Auto-Merge
        if: steps.current.outputs.version != steps.fetch-release.outputs.version && steps.fetch-release.outputs.version != '' && steps.fetch-release.outputs.version != 'null' && steps.cpr.outputs.pull-request-number
        run: |
          # Wait for status checks to complete (max 2 minutes)
          for i in {1..24}; do
            status=$(gh pr view "${{ steps.cpr.outputs.pull-request-number }}" --json mergeable -q .mergeable)
            if [ "$status" = "MERGEABLE" ]; then
              gh pr merge --auto --merge "${{ steps.cpr.outputs.pull-request-number }}"
              break
            fi
            sleep 5
          done
        env:
          GH_TOKEN: ${{ secrets.PAT }} 